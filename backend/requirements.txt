fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
pydantic[email]>=2.6.0
pydantic-settings>=2.2.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt is pinned to 3.2.2 because passlib 1.7.4 is not compatible with
# bcrypt >= 4.0 (the bcrypt package dropped the __about__ module that passlib
# introspects at import time, causing an AttributeError on startup).
# passlib is effectively unmaintained; the pin keeps the stack working until
# a migration to bcrypt's native API (bcrypt.hashpw / checkpw) is done.
# Tracking issue: https://github.com/pyca/bcrypt/issues/684
bcrypt==3.2.2
redis[asyncio]>=5.0.0
python-multipart>=0.0.9
httpx>=0.27.0
slowapi>=0.1.9
